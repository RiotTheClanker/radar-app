<?xml version="1.0" encoding="utf-8"?>
<!-- Drawn rather than shipped as a PNG so it stays crisp at any density.
     Same two stops as the plate in branding/icon.svg. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="315"
        android:startColor="#0E1626"
        android:endColor="#1E3255"
        android:type="linear"/>
</shape>
